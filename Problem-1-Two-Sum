from typing import List
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        num={}
        for i in range(len(nums)):
            c=target-nums[i]
            if c in num:
                return [num[c],i]
            num[nums[i]]=i
        return []
