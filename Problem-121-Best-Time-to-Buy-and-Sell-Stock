from typing import List
class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        maxprof=0
        min=10**4
        for i in prices:
            if min>i:
                min=i
            profit=i-min
            maxprof=max(maxprof,profit)
        return maxprof